services:
  - type: web              # free tier — Web Service (not Worker)
    name: italiano-bot
    runtime: docker        # use our Dockerfile
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: BOT_TOKEN
        sync: false        # paste manually in Render dashboard
